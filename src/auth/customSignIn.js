import axios from 'axios'
export const mySignInFunction = (data) => {
    // TODO Handle ID checking, Post request for checks. Add to U
    // TODO Add to user table, team table if new. Make Pivot as well. 
    return data.id
} 